{
  "name": "testchangeversion",
  "version": "1.0.0",
  "description": "測試提交記錄到master時自動更新版號",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "husky": {
    "hooks": {
      "post-merge": "npm version patch --no-git-tag-version",
      "pre-push": "npm version patch --no-git-tag-version"
    }
  }
}
