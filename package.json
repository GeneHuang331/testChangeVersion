{
  "name": "testchangeversion",
  "version": "1.1.0",
  "description": "測試提交記錄到master時自動更新版號",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "husky": {
    "hooks": {
      "post-merge": "if [ $(git rev-parse --abbrev-ref HEAD) = 'main' ]; then npm version patch --no-git-tag-version; fi",
      "pre-push": "if [ $(git rev-parse --abbrev-ref HEAD) = 'main' ]; then npm version patch --no-git-tag-version; fi"
    }
  }
}
