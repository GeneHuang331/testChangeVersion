{
  "name": "testchangeversion",
  "version": "1.1.0",
  "description": "測試提交記錄到master時自動更新版號",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "husky": {
    "hooks": {
      "pre-push": "if [ $(git rev-parse --abbrev-ref HEAD) = 'main' ]; then npm version minor --no-git-tag-version; fi"
    }
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
 
}
